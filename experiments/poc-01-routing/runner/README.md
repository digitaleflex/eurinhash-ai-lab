# Runner

The runner must execute all baselines under the same workload, capture per-request telemetry, write machine-readable results, and make the run reproducible from a recorded configuration and seed.
